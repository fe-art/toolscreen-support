# Toolscreen Troubleshooting Decision Tree
# Format: flat node graph with ID-based references
#
# Node types:
#   question  - Ask user, show buttons, branch on answer
#   solution  - Show fix, then "Did this help?" -> yes=resolved / no=did_not_help
#   info      - Show text, then auto-continue to `next`
#   escalate  - Collect diagnostics sequentially, then open bug report
#
# Every `solution` node MUST have `did_not_help` pointing to the next node.
# Every `question` option MUST have `next`.
# `escalate` is always a terminal node.

meta:
  version: 4
  updated: "2026-03-17"

nodes:

  # ============================================================
  # ROOT
  # ============================================================
  root:
    type: question
    text: "What issue are you experiencing?"
    options:
      - label: "Toolscreen won't start / crashes"
        next: cat_startup
      - label: "Display & screen issues"
        next: cat_display
      - label: "Overlays & features"
        next: cat_overlays
      - label: "Input, cursor & performance"
        next: cat_input_perf
      - label: "Settings, setup & FAQ"
        next: cat_settings_faq
      - label: "Something else"
        next: escalate_generic

  # ============================================================
  # CATEGORY NODES
  # ============================================================
  cat_startup:
    type: question
    text: "What startup / crash issue are you experiencing?"
    options:
      - label: "Toolscreen doesn't show up / Ctrl+I does nothing"
        next: nsu_worked_before
      - label: "Game crashes or won't launch"
        next: crash_when
      - label: "Double-clicking the .jar does nothing / opens as a ZIP"
        next: sol_jarfix

  cat_display:
    type: question
    text: "What display or screen issue are you experiencing?"
    options:
      - label: "Screen freezes, goes black, or flickers"
        next: ff_when
      - label: "Toolscreen doesn't fill the screen (1440p, 4K, ultrawide)"
        next: sol_resolution_mismatch
      - label: "Fullscreen resets on every new world"
        next: sol_ff_standardsettings
      - label: "EyeZoom overlay is completely white / mirrors disappeared"
        next: eyezoom_white_version
      - label: "Game is stuck in EyeZoom / Thin mode on every launch"
        next: sol_mode_stuck_default
      - label: "\"Go Borderless\" button / click-to-borderless popup disappeared"
        next: sol_borderless_popup_gone
      - label: "Transparent background in fullscreen (is it possible?)"
        next: sol_faq_transparent_fullscreen

  cat_overlays:
    type: question
    text: "Which overlay or feature has an issue?"
    options:
      - label: "Eye zoom / sensitivity issue"
        next: sens_type
      - label: "NinjaBrainBot overlay setup or issue"
        next: ninb_setup_or_issue
      - label: "Pie mirror / overlay not updating"
        next: ov_pie_or_other
      - label: "Pie chart looks doubled in thin mode"
        next: sol_thin_pie_doubled
      - label: "Pie chart resets directory every new world"
        next: sol_pie_directory_standardsettings
      - label: "OBS / screen recording issue"
        next: obs_symptom

  cat_input_perf:
    type: question
    text: "What input or performance issue are you experiencing?"
    options:
      - label: "Performance issues (FPS drop, lag)"
        next: perf_severity
      - label: "Keybind / hotkey not working"
        next: kb_type
      - label: "Custom cursor or background image won't load"
        next: sol_cursor_unicode_path
      - label: "Mouse cursor disappears from the game window"
        next: sol_cursor_disappears

  cat_settings_faq:
    type: question
    text: "What settings or setup question do you have?"
    options:
      - label: "Settings not saving"
        next: save_which
      - label: "How do I update / uninstall Toolscreen?"
        next: howto_update_or_uninstall
      - label: "I'm on Linux"
        next: sol_linux_waywall
      - label: "GIF background / does Toolscreen support GIF?"
        next: sol_faq_gif_background
      - label: "Auto-launch NinjaBrainBot with Toolscreen"
        next: sol_faq_autolaunch_ninb
      - label: "Minecraft 1.21.x support / terrain breaks on resize"
        next: sol_faq_mc121

  # ============================================================
  # BRANCH 1: Toolscreen doesn't show up
  # ============================================================
  nsu_worked_before:
    type: question
    text: "Did Toolscreen work before but stopped after shutting down / restarting your PC?"
    options:
      - label: "Yes, it worked before"
        next: sol_nsu_first_boot_fail
      - label: "No, it never worked"
        next: nsu_fullscreen

  sol_nsu_first_boot_fail:
    type: solution
    text: |
      Some users report Toolscreen not loading on the **first launch after a PC restart**.
      This is a **known intermittent issue** under investigation.

      **Steps to try:**
      1. Close Minecraft completely and relaunch it (may take 2-3 tries)
      2. Check that your antivirus hasn't deleted files:
         - The .jar should be in your instance folder
         - The DLL should be in `C:\Users\<username>\.config\toolscreen\dlls\`
      3. Update to the latest Toolscreen version
    did_not_help: nsu_antivirus

  nsu_fullscreen:
    type: question
    text: "What Toolscreen version are you using?"
    options:
      - label: "1.1 or newer"
        next: sol_use_fullscreen_v11
      - label: "1.0.9 or older"
        next: sol_use_fullscreen_legacy
      - label: "Not sure"
        next: sol_use_fullscreen_v11

  sol_use_fullscreen_v11:
    type: solution
    text: |
      Since **v1.1**, Toolscreen works in **fullscreen**, **borderless**, and **windowed** mode.
      Press **F11** to enter fullscreen, or use one of these borderless options:
      1. **Ctrl+I -> Basic -> Go Borderless** button
      2. Enable **auto-borderless** in the Basic tab
      3. Set a **borderless hotkey** in Advanced -> Hotkeys -> Toggle Borderless
    did_not_help: nsu_mc_open

  sol_use_fullscreen_legacy:
    type: solution
    text: |
      On versions **before v1.1**, Toolscreen only works in **fullscreen** or **borderless** mode.
      Press **F11** to enter fullscreen. The config menu (Ctrl+I) only works in fullscreen.
      **Update Toolscreen** to v1.1+ for windowed mode support.
    did_not_help: nsu_mc_open

  nsu_mc_open:
    type: question
    text: "Was **Minecraft open** when you installed Toolscreen?"
    options:
      - label: "Yes / Not sure"
        next: sol_mc_open_during_install
      - label: "No, MC was closed"
        next: nsu_antivirus

  sol_mc_open_during_install:
    type: solution
    text: |
      Toolscreen cannot install properly while Minecraft is running.
      1. **Close Minecraft** completely
      2. **Re-run** the Toolscreen installer (double-click the .jar)
      3. Launch Minecraft and check if Toolscreen appears
    did_not_help: nsu_antivirus

  nsu_antivirus:
    type: question
    text: "What antivirus software are you using?"
    options:
      - label: "Windows Defender (default)"
        next: nsu_smart_app_control
      - label: "Third-party (McAfee, Bitdefender, Norton, Kaspersky, Avast, etc.)"
        next: sol_thirdparty_av
      - label: "None / Disabled"
        next: nsu_afterburner

  nsu_smart_app_control:
    type: question
    text: "Are you on **Windows 11**? If so, is **Smart App Control** enabled?"
    options:
      - label: "Yes / Not sure (Windows 11)"
        next: sol_disable_smart_app_control
      - label: "No, I'm on Windows 10"
        next: nsu_defender_exclusions
      - label: "Already disabled"
        next: nsu_defender_exclusions

  sol_disable_smart_app_control:
    type: solution
    text: |
      **Smart App Control** silently blocks Toolscreen even with Defender exclusions set.
      **Toolscreen v1.1+** will attempt to detect SAC and guide you through disabling it.

      To disable manually:
      1. Press **Windows + I** to open Settings
      2. Go to **Privacy & Security > Windows Security > App & Browser Control**
      3. Set **Smart App Control** to **Off**
      4. Under Reputation-based Protection, turn off **"Check Apps and Files"**
      5. Restart Minecraft and try again

      This is separate from Windows Defender and must be disabled independently.
      This setting does NOT exist on Windows 10.
    did_not_help: nsu_defender_exclusions

  nsu_defender_exclusions:
    type: question
    text: "Did you add Toolscreen exclusions in Windows Defender during install?"
    options:
      - label: "No / Not sure"
        next: sol_add_defender_exclusions
      - label: "Yes, exclusions are set"
        next: nsu_afterburner

  sol_add_defender_exclusions:
    type: solution
    text: |
      Reinstall Toolscreen and **accept the exclusion prompts**.
      If you skipped them, manually add exclusions for:
      - The Toolscreen JAR file
      - `C:\Users\<username>\.config\toolscreen\dlls\`
      - `C:\Users\<username>\AppData\Local\Temp\Toolscreen\`
    did_not_help: nsu_afterburner

  sol_thirdparty_av:
    type: solution
    text: |
      Third-party AV programs (McAfee, Bitdefender, Norton, etc.) often **quarantine** Toolscreen.dll
      on first encounter. "Disabling" the AV does NOT restore quarantined files.

      **Steps to try in order:**
      1. Open your AV's **quarantine/history** and **restore** any Toolscreen-related files
      2. Add exclusions for `C:\Users\<username>\.config\toolscreen\` in the AV
      3. Re-run the Toolscreen installer (double-click the .jar)
      4. If files still won't save/load after restoring, your AV may have changed folder permissions.
         Run in **Admin PowerShell**: `icacls "$env:USERPROFILE\.config\Toolscreen" /reset /t`
      5. If that fails, **fully uninstall** the third-party AV (not just disable it).
         Windows Defender will take over and respects Toolscreen exclusions properly.
    did_not_help: nsu_afterburner

  nsu_afterburner:
    type: question
    text: "Do you have **MSI Afterburner** or **RivaTuner** running?"
    options:
      - label: "Yes"
        next: sol_close_afterburner
      - label: "No"
        next: nsu_gpu_overlay

  sol_close_afterburner:
    type: solution
    text: |
      Close **MSI Afterburner AND RivaTuner** completely. They conflict with Toolscreen's graphics hooks.

      If you need Afterburner, you can either:
      - Add Minecraft's `javaw.exe` to RivaTuner and set **Application Detection Level** to **None**
      - Hook chaining is **enabled by default** since v1.1 and should handle most overlay conflicts automatically

      **Also check `config.toml`:** If `disableHookChaining = true` is set, the GUI (Ctrl+I) will not open even if everything else is correct. Open `C:\Users\<username>\.config\toolscreen\config.toml` and ensure `disableHookChaining = false` (the default since v1.1). Only set it to `true` if you experience crashes with RTSS.
    did_not_help: nsu_gpu_overlay

  nsu_unicode:
    type: question
    text: "Does your **Windows username** contain non-English characters (accents, special symbols, etc.)?"
    options:
      - label: "Yes"
        next: sol_unicode_username
      - label: "No"
        next: nsu_launcher

  sol_unicode_username:
    type: solution
    text: |
      This was a bug fixed in **v1.0.6**. **Update Toolscreen** to the latest version.
    did_not_help: nsu_launcher

  nsu_gpu_overlay:
    type: question
    text: "Do you have any **GPU overlay software** running? (GeForce Experience, AMD Adrenalin overlay, Discord overlay)"
    options:
      - label: "Yes"
        next: sol_disable_gpu_overlay
      - label: "No"
        next: nsu_unicode

  sol_disable_gpu_overlay:
    type: solution
    text: |
      Overlay software can conflict with Toolscreen's graphics hooks, just like RivaTuner.
      **Disable overlays** in:
      - **GeForce Experience** -> Settings -> In-Game Overlay -> OFF
      - **AMD Adrenalin** -> Settings -> Preferences -> In-Game Overlay -> OFF
      - **Discord** -> Settings -> Game Overlay -> OFF
      - **Medal** -> Close entirely (known conflicts with Toolscreen)
    did_not_help: nsu_java_path

  nsu_java_path:
    type: question
    text: "Check your launcher's Java path: **Edit Instance > Settings > Java**. Does it show just **\"javaw\"** or a full path starting with **C:/...**?"
    options:
      - label: "It just says 'javaw'"
        next: sol_fix_java_path
      - label: "It shows a full path (C:/Program Files/Java/...)"
        next: nsu_unicode

  sol_fix_java_path:
    type: solution
    text: |
      This was fixed in **v1.0.9+**. **Update Toolscreen** to the latest version.

      If you can't update, fix the Java path manually:
      1. Go to **Edit Instance > Settings > Java > Auto-detect** (or Browse)
      2. Select a path that starts with `C:/...` such as:
         `C:/Program Files/Java/jdk-21/bin/javaw.exe`
      3. If no full path appears, install Java from: https://aka.ms/download-jdk/microsoft-jdk-21-windows-x64.msi
      4. After installing, click **Refresh** in the Auto-detect menu

      Note: If using **GraalVM**, make sure your instance uses `javaw.exe`, not `java.exe`.
    did_not_help: nsu_unicode

  nsu_launcher:
    type: question
    text: "What launcher are you using?"
    options:
      - label: "Vanilla (official Mojang) launcher"
        next: sol_vanilla_launcher
      - label: "Prism Launcher / MultiMC"
        next: sol_nsu_clean_reinstall
      - label: "Modrinth App"
        next: sol_modrinth_unsupported
      - label: "MCSR Launcher"
        next: sol_mcsr_launcher_issues

  sol_vanilla_launcher:
    type: solution
    text: |
      Toolscreen **does not support** the vanilla launcher. The installer uses pre-launch commands that the vanilla launcher can't run.
      Switch to **Prism Launcher** or **MultiMC**.
    did_not_help: escalate_generic

  sol_modrinth_unsupported:
    type: solution
    text: |
      The **Modrinth App** is not yet supported by Toolscreen. Switch to **Prism Launcher** or **MultiMC** for full compatibility.
    did_not_help: escalate_generic

  sol_mcsr_launcher_issues:
    type: solution
    text: |
      The **MCSR Launcher** has known issues with Toolscreen:
      - It sometimes fails to create `Toolscreen.jar`
      - The auto-updater does not work properly

      **Recommended:** Switch to **Prism Launcher** for the most reliable Toolscreen experience.
      If you want to stay on MCSR Launcher, manually download the .jar and place it in your instance folder.
    did_not_help: sol_nsu_clean_reinstall

  sol_nsu_clean_reinstall:
    type: solution
    text: |
      Try a clean reinstall:
      1. Delete the config folder: `C:\Users\<username>\.config\toolscreen\`
      2. Reinstall Toolscreen
      3. Verify the **pre-launch command** is set correctly in your instance settings
      4. Check your launcher logs for errors
    did_not_help: escalate_generic

  # ============================================================
  # BRANCH 2: Screen freezes / goes black / flickers
  # ============================================================
  ff_when:
    type: question
    text: "When does the issue occur?"
    options:
      - label: "When loading a new world / starting a new run"
        next: sol_ff_standardsettings
      - label: "When toggling a hotkey/macro (resize, thin, borderless)"
        next: ff_toggle_gpu
      - label: "When switching between fullscreen and windowed/borderless"
        next: ff_toggle_gpu
      - label: "Constantly or randomly"
        next: ff_random_mirrors

  sol_ff_standardsettings:
    type: solution
    text: |
      This is usually caused by the **Standard Settings mod** toggling fullscreen off on each new world.
      1. Check your **standardsettings.json** file in your instance folder
      2. Look for a `fullscreen` setting and make sure it's set to **true** (or remove the line)
      3. Alternatively, open Minecraft settings (book and quill icon) and verify fullscreen is enabled there
    did_not_help: ff_toggle_gpu

  # --- Branch 2A: triggered by toggle ---
  ff_toggle_gpu:
    type: question
    text: "What type of GPU do you have?"
    options:
      - label: "Integrated (Intel HD, Intel Iris, AMD integrated)"
        next: sol_ff_integrated_gpu
      - label: "Dedicated (NVIDIA or AMD discrete)"
        next: sol_ff_dedicated_gpu
      - label: "Not sure"
        next: sol_ff_integrated_gpu

  sol_ff_integrated_gpu:
    type: solution
    text: |
      This is a **known bug** strongly correlated with integrated graphics. Try the following in order:
      1. **Update to Toolscreen v1.0.5+** (rendering changes fix this for many users)
      2. Add `C:\Users\<username>\.config\toolscreen\` to **Windows Defender exclusions**
      3. Enable **"Allow exit to fullscreen regardless of game state"** in settings
      4. Delete `config.toml` and reinstall
    did_not_help: escalate_freeze

  sol_ff_dedicated_gpu:
    type: question
    text: "Is your dedicated GPU **AMD** or **NVIDIA**?"
    options:
      - label: "AMD"
        next: sol_ff_dedicated_amd
      - label: "NVIDIA"
        next: sol_ff_dedicated_nvidia

  sol_ff_dedicated_amd:
    type: solution
    text: |
      AMD GPU drivers (especially recent ones) can cause freezes when toggling macros.
      Try these fixes in order:
      1. **Roll back your AMD drivers** to an older version (e.g., 25.9.1 or earlier) — this has fixed it for many users
      2. **Close Medal** if running (known to conflict with Toolscreen)
      3. If using OBS, check that **Window Capture** sources use **BitBlt** capture method, not "Windows 10" — the Windows 10 mode can cause freezes
      4. **Update Toolscreen** to the latest version
      5. Add `C:\Users\<username>\.config\toolscreen\` to **Windows Defender exclusions**
      6. Enable **"Allow exit to fullscreen regardless of game state"**
      7. Delete `config.toml` and reinstall
    did_not_help: escalate_freeze

  sol_ff_dedicated_nvidia:
    type: solution
    text: |
      Less common on dedicated NVIDIA GPUs, but still possible. Try these steps:
      1. **Update to Toolscreen v1.0.5+**
      2. Add `C:\Users\<username>\.config\toolscreen\` to **Windows Defender exclusions**
      3. Enable **"Allow exit to fullscreen regardless of game state"**
      4. Delete `config.toml` and reinstall
      5. **Update your GPU drivers**
    did_not_help: escalate_freeze

  escalate_freeze:
    type: escalate
    text: |
      This is an acknowledged bug that the developer is investigating. Let's collect information for a bug report.
      Please also enable **Advanced Debug Logging**: Advanced -> Settings -> Debug (5739) -> Advanced Logging, then reproduce the issue.
    collect:
      - key: gpu
        prompt: "What GPU do you have? (exact model, e.g., Intel HD 4600, RX 9070 XT)"
      - key: ts_version
        prompt: "What Toolscreen version?"
      - key: display_mode
        prompt: "What display mode? (Fullscreen / Borderless)"
      - key: trigger
        prompt: "What exactly triggers the freeze? (which hotkey/macro)"
      - key: log
        prompt: "Please upload your Toolscreen log: `C:\\Users\\<username>\\.config\\toolscreen\\logs\\latest.log`"
      - key: launcher_log
        prompt: "Please upload your launcher log (from Prism/MultiMC log viewer)"

  # --- Branch 2B: constant/random flickering ---
  ff_random_mirrors:
    type: question
    text: "Do you have **mirrors** enabled in your current mode (especially the **mapless mirror**)?"
    options:
      - label: "Yes"
        next: sol_remove_mirror
      - label: "No"
        next: ff_random_version
      - label: "Not sure"
        next: sol_remove_mirror

  sol_remove_mirror:
    type: solution
    text: |
      Try **removing the mapless mirror** from your current mode's configuration.
      This is a confirmed bug where mirrors cause flickering. This was potentially fixed in **v1.0.9** — **update Toolscreen** to the latest version.
    did_not_help: ff_random_version

  ff_random_version:
    type: question
    text: "What Toolscreen version are you using?"
    options:
      - label: "1.0.3 or older"
        next: sol_update_flickering
      - label: "1.0.5 or newer"
        next: ff_random_displaymode

  sol_update_flickering:
    type: solution
    text: |
      Version 1.0.3 has known flickering issues. **Update to the latest version** of Toolscreen.
    did_not_help: ff_random_displaymode

  ff_random_displaymode:
    type: question
    text: "Are you using fullscreen or borderless mode?"
    options:
      - label: "Borderless"
        next: sol_switch_to_fullscreen
      - label: "Fullscreen"
        next: ff_mojang_logo

  sol_switch_to_fullscreen:
    type: solution
    text: |
      Flickering is more common in **borderless** mode. Try switching to **fullscreen** and see if the issue persists.
    did_not_help: ff_mojang_logo

  ff_mojang_logo:
    type: question
    text: "Does the screen flash the **Mojang logo** during the flickering?"
    options:
      - label: "Yes"
        next: sol_mojang_flash
      - label: "No"
        next: escalate_generic

  sol_mojang_flash:
    type: solution
    text: |
      This is a rare bug. Check if Toolscreen's **pre-launch command** is being reset by your launcher.
      Verify the pre-launch command in your Prism/MultiMC instance settings is still intact.
    did_not_help: escalate_generic

  # ============================================================
  # BRANCH 3: Game crashes or won't launch
  # ============================================================
  crash_when:
    type: question
    text: "When does the crash occur?"
    options:
      - label: "Jar file disappeared / 'unable to access jarfile'"
        next: sol_crash_jar_missing
      - label: "Immediately on launch"
        next: crash_launch_code
      - label: "When pressing F11 (fullscreen toggle)"
        next: sol_crash_f11
      - label: "After 5-15 minutes of playing"
        next: sol_crash_delayed
      - label: "Launcher says version/mod loader error after updating Toolscreen"
        next: sol_crash_modloader_entry
      - label: "When using a specific feature (resize, overlay, etc.)"
        next: crash_feature

  sol_crash_jar_missing:
    type: solution
    text: |
      Your Toolscreen .jar was likely **deleted by antivirus**.

      Toolscreen v1.0.3 was recently classified as a trojan by Microsoft and Google
      malware definitions (false positive). Newer versions (1.0.8+) are NOT affected.

      **Fix:**
      1. Download the **latest version** of Toolscreen (not 1.0.3)
      2. If you can't open the new .jar, **restart your PC** first
      3. Double-click the new .jar to install
      4. If it persists, temporarily disable Windows Defender real-time protection,
         install, then re-enable Defender
    did_not_help: nsu_antivirus

  sol_crash_modloader_entry:
    type: solution
    text: |
      Toolscreen should NOT appear in your launcher's mod loader list. If it does, it was
      added during a previous botched install and is preventing your game from launching.

      **Fix:**
      1. In your launcher, go to **Edit Instance > Version**
      2. Look for any Toolscreen entry (e.g., "Toolscreen 1.0.3")
      3. Select it and click **Remove** on the right side
      4. Launch the game and reinstall Toolscreen normally
    did_not_help: crash_launch_code

  sol_crash_f11:
    type: solution
    text: |
      Pressing **F11** while Toolscreen is active can crash the game. This is a known issue.
      Use Toolscreen's own **borderless hotkey** instead of F11 to toggle display modes:
      - Open Ctrl+I > **Advanced > Hotkeys > Toggle Borderless** and assign a hotkey
    did_not_help: escalate_crash

  # --- 3A: crash on launch ---
  crash_launch_code:
    type: question
    text: "Do you see an exit code in the crash log? Check your launcher's log viewer."
    options:
      - label: "Exit code -1073741819 (0xC0000005)"
        next: sol_crash_access_violation
      - label: "Exit code -805306369"
        next: sol_crash_805
      - label: "White screen crash"
        next: sol_crash_white_screen
      - label: "Other code / No code / Not sure"
        next: sol_crash_generic

  sol_crash_access_violation:
    type: solution
    text: |
      Exit code `-1073741819` (Access Violation). Try these fixes in order:
      1. **Disable Hardware Acceleration** in Discord
      2. Close OBS, or switch from **Game Capture** to **Window Capture**
      3. If Jingle is running, make sure it's using **Java 17+**
      4. Remove the **Sodium** mod if installed
      5. Check for and update any **outdated mods**
    did_not_help: sol_crash_generic

  sol_crash_805:
    type: solution
    text: |
      Exit code `-805306369` is often caused by outdated mods or killing Minecraft while frozen.
      1. **Update all mods** to their latest versions
      2. **Update Toolscreen** to the latest version
      3. Try launching with a **clean instance** (no extra mods)
    did_not_help: sol_crash_generic

  sol_crash_white_screen:
    type: solution
    text: |
      White screen crashes are typically caused by the **state-output** mod.
      1. Check if `state-output` is installed in your mods folder
      2. Delete the file `wpstateout.txt` from your instance folder
      3. If the crash persists, remove the state-output mod entirely
    did_not_help: sol_crash_generic

  sol_crash_generic:
    type: solution
    text: |
      Try a clean slate:
      1. Delete the config folder: `C:\Users\<username>\.config\toolscreen\`
      2. Reinstall Toolscreen
      3. Make sure you're using **Java 17+** (Adoptium/Temurin recommended)
      4. If you're using **GraalVM**, switch to Adoptium/Temurin (known incompatibility)
      5. Remove any unnecessary mods and test with a minimal setup
    did_not_help: escalate_crash

  # --- 3B: delayed crash ---
  sol_crash_delayed:
    type: solution
    text: |
      Crashes after 5-15 minutes often correlate with:
      1. **GPU driver issues** (especially Intel integrated) — update your GPU drivers
      2. **Over-allocated RAM** — don't give Minecraft too much memory (try 2-3GB)
      3. **Old Toolscreen version** — update to the latest version

      Try all three and see if the crash recurs.
    did_not_help: escalate_crash

  # --- 3C: feature-triggered crash ---
  crash_feature:
    type: question
    text: "Which feature triggers the crash?"
    options:
      - label: "Resizing (thin/wide modes)"
        next: sol_crash_resize
      - label: "NinjaBrainBot overlay with interaction enabled"
        next: sol_crash_ninb_overlay
      - label: "Full computer crash / BSOD (not just Minecraft)"
        next: sol_crash_full_pc
      - label: "Other feature"
        next: sol_crash_generic

  sol_crash_full_pc:
    type: solution
    text: |
      Full PC crashes / BSODs with Toolscreen are rare but serious. They are most commonly caused by:
      1. **GPU driver conflicts** — update to the latest GPU drivers
      2. **Overlay software conflicts** — close MSI Afterburner, RivaTuner, GeForce Experience overlay
      3. **Very new GPU hardware** may have driver incompatibilities with Toolscreen's graphics hooks

      Try:
      1. Update GPU drivers to the latest version
      2. Disable all overlay software
      3. Update Toolscreen to the latest version
      4. If using resizing features, update to v1.0.5+ (resizing crashes were fixed)
    did_not_help: escalate_crash

  sol_crash_resize:
    type: solution
    text: |
      Resizing crashes (including full system crashes) were a known bug fixed in **Toolscreen v1.0.5+**.
      **Update Toolscreen** to the latest version.
    did_not_help: escalate_crash

  sol_crash_ninb_overlay:
    type: solution
    text: |
      Crashes with NinjaBrainBot overlay interaction enabled were a known bug fixed in **Toolscreen v1.0.6+**.
      **Update Toolscreen** to the latest version.
    did_not_help: escalate_crash

  escalate_crash:
    type: escalate
    text: |
      Let's collect crash information for a bug report.
    collect:
      - key: os
        prompt: "What OS are you on? (e.g., Windows 11 23H2)"
      - key: ts_version
        prompt: "What Toolscreen version?"
      - key: mc_version
        prompt: "What Minecraft version?"
      - key: java
        prompt: "What Java version? (must be 17+)"
      - key: gpu
        prompt: "What GPU do you have?"
      - key: mods
        prompt: "List your installed mods (or say 'none')"
      - key: log
        prompt: "Please upload your Toolscreen log: `C:\\Users\\<username>\\.config\\toolscreen\\logs\\latest.log`"
      - key: launcher_log
        prompt: "Please upload your launcher log"
      - key: steps
        prompt: "Steps to reproduce the crash?"

  # ============================================================
  # BRANCH 4: Performance issues (FPS drop, lag)
  # ============================================================
  perf_severity:
    type: question
    text: "How would you describe the performance issue?"
    options:
      - label: "Massive FPS drop (e.g., 60 -> 10 FPS)"
        next: perf_massive
      - label: "FPS halved after going \"borderless\" (snapped/maximised window)"
        next: sol_perf_pseudowindowed
      - label: "Frame loss on every mouse movement"
        next: perf_polling_rate
      - label: "FPS degrades gradually over time"
        next: sol_perf_gradual
      - label: "Lag only in a specific mode (eye zoom, thin, etc.)"
        next: perf_font
      - label: "FPS significantly lower with Toolscreen than without"
        next: sol_perf_mapless_mirror
      - label: "Virtual camera causing FPS drop"
        next: sol_obs_virtual_camera_fps
      - label: "Overlay is slow/laggy (NinjaBrainBot)"
        next: perf_overlay_type

  perf_polling_rate:
    type: question
    text: "Is your **mouse polling rate** set above 1000Hz?"
    options:
      - label: "Yes (2000Hz, 4000Hz, 8000Hz, etc.)"
        next: sol_perf_polling_rate
      - label: "No / 1000Hz or below / Not sure"
        next: perf_font

  sol_perf_polling_rate:
    type: solution
    text: |
      Minecraft has known performance issues with mouse polling rates above **1000Hz**.
      This becomes more noticeable with Toolscreen's rendering.
      **Lower your mouse polling rate to 1000Hz or below** and test again.
    did_not_help: perf_font

  perf_massive:
    type: question
    text: "Are you on a **low-end system** (e.g., older laptop, limited RAM)?"
    options:
      - label: "Yes"
        next: sol_perf_ram
      - label: "No, my system should handle it"
        next: perf_font

  sol_perf_ram:
    type: solution
    text: |
      On low-end systems, **don't over-allocate RAM** to Minecraft. Leave enough resources for Toolscreen.
      Try setting Minecraft to **2-3 GB max** in your launcher's memory settings.
    did_not_help: perf_font

  sol_perf_gradual:
    type: solution
    text: |
      Gradual FPS degradation suggests a memory leak or config corruption.
      1. **Update your GPU drivers** (check via F3 screen in-game)
      2. Delete the config folder: `C:\Users\<username>\.config\toolscreen\`
      3. Restart and test again
      4. If it only happens in **ranked** but not practice, note that for the bug report
    did_not_help: perf_font

  perf_font:
    type: question
    text: "Did you select a **custom font** in Toolscreen settings that might be missing or invalid?"
    options:
      - label: "Yes / Maybe"
        next: sol_perf_font_reset
      - label: "No"
        next: perf_border

  sol_perf_font_reset:
    type: solution
    text: |
      Invalid fonts cause severe lag (fixed in **v1.0.9**). **Update Toolscreen** to the latest version.
      Delete the config file to reset the font:
      `C:\Users\<username>\.config\toolscreen\config.toml`
    did_not_help: perf_border

  perf_border:
    type: question
    text: "Are you using a **dynamic border** with **high thickness** values?"
    options:
      - label: "Yes"
        next: sol_perf_static_border
      - label: "No / Not sure"
        next: perf_overlay_type

  sol_perf_static_border:
    type: solution
    text: |
      The dynamic border rendering algorithm doesn't scale to high thickness values.
      Switch the border type from **dynamic** to **static** in your mode settings.
    did_not_help: perf_overlay_type

  perf_overlay_type:
    type: question
    text: "Is the lag specifically in the **NinjaBrainBot overlay**?"
    options:
      - label: "Yes, the overlay updates slowly"
        next: perf_overlay_image_vs_window
      - label: "No, the whole game is laggy"
        next: escalate_generic

  perf_overlay_image_vs_window:
    type: question
    text: "Are you using an **image overlay** or a **window overlay** for NinjaBrainBot?"
    options:
      - label: "Image overlay"
        next: sol_perf_switch_to_window_overlay
      - label: "Window overlay"
        next: sol_perf_ninb_not_minimized
      - label: "Not sure"
        next: sol_perf_switch_to_window_overlay

  sol_perf_switch_to_window_overlay:
    type: solution
    text: |
      The **image overlay** has a hardcoded **1-second update delay**. Switch to the **window overlay** for real-time updates.
      Check the pinned messages in the setup channel for instructions on switching.
    did_not_help: escalate_generic

  sol_perf_ninb_not_minimized:
    type: solution
    text: |
      Make sure NinjaBrainBot is **not minimized**. When minimized, the window overlay stops receiving updates.
      On a single monitor, use **Win+Tab** to put NinjaBrainBot on a **separate virtual desktop** instead of minimizing it.
    did_not_help: escalate_generic

  # ============================================================
  # BRANCH 5: Keybind / hotkey not working
  # ============================================================
  kb_type:
    type: question
    text: "What kind of keybind issue are you experiencing?"
    options:
      - label: "Mouse side buttons (MB4/MB5) not registering"
        next: sol_kb_mouse_buttons
      - label: "Modifier keys (Ctrl, Shift, Alt) can't be rebound"
        next: sol_kb_modifier_keys
      - label: "Key combination is blocked (e.g., Shift+F3 + another key)"
        next: sol_kb_exclusion_keys
      - label: "Hotkey triggers when I don't want it to (while typing, in inventory)"
        next: sol_kb_game_states
      - label: "Hotkey conflicts with pie chart / number keys"
        next: sol_kb_number_keys
      - label: "Hotkey requires multiple presses / modes cycle weirdly"
        next: sol_kb_jingle_conflict
      - label: "Caps Lock or Windows key can't be rebound"
        next: sol_kb_capslock_winkey
      - label: "Backspace clears the bind instead of being assigned as the trigger"
        next: sol_kb_backspace_bind
      - label: "\"Types\" field shows wrong character (keyboard layout editor)"
        next: sol_kb_types_wrong_char
      - label: "Holding D (strafe) blocks a mouse button hotkey from firing"
        next: sol_kb_d_blocks_mouse_f3
      - label: "No way to pause all rebinds while typing in chat"
        next: sol_kb_typing_toggle
      - label: "Shift+key alternative mode fires incorrectly"
        next: sol_mode_alt_hotkey_priority
      - label: "Other keybind issue"
        next: kb_other

  sol_kb_mouse_buttons:
    type: solution
    text: |
      Mouse button rebinding **works since v1.0.4**, but has quirks:
      - Chained rebinds (mouse -> key A, key A -> key B) will follow the chain
      - Some combinations behave unexpectedly with multiple rebinds active

      If mouse buttons don't register at all, **update Toolscreen** to the latest version.
      If you hit a quirk, use **AutoHotKey (AHK)** to remap mouse buttons externally instead.
    did_not_help: escalate_generic

  sol_kb_modifier_keys:
    type: question
    text: "What Toolscreen version are you using?"
    options:
      - label: "1.1 or newer"
        next: sol_kb_modifier_keys_v11
      - label: "1.0.9 or older"
        next: sol_kb_modifier_keys_legacy
      - label: "Not sure"
        next: sol_kb_modifier_keys_v11

  sol_kb_modifier_keys_v11:
    type: solution
    text: |
      Modifier key rebinding was **fixed in v1.1**. Left Alt vs Right Alt was fixed in **v1.1.2**.

      If rebinding still doesn't work:
      1. Use the **keyboard layout editor** in Advanced -> Inputs for advanced rebinding
      2. If a specific modifier still won't bind, use **AutoHotKey (AHK)** as a workaround
    did_not_help: escalate_generic

  sol_kb_modifier_keys_legacy:
    type: solution
    text: |
      On versions **before v1.1**, modifier keys (RSHIFT, CAPSLOCK, RALT, LALT) cannot be rebound reliably.
      **Update Toolscreen** to v1.1+ to fix this.
      If you can't update, use **AutoHotKey (AHK)** to remap these keys externally.
    did_not_help: escalate_generic

  sol_kb_exclusion_keys:
    type: solution
    text: |
      A key in the **exclusion list** is likely blocking your combination.
      Go to **Advanced -> Hotkeys -> [Your Hotkey] -> Exclusion Keys** and check if any of the keys you're trying to use simultaneously are listed there.
      Remove the conflicting key from the exclusion list.

      **F3 is added as a default exclusion key** since v1.0.3. This means pressing F3 + your hotkey
      won't trigger the hotkey. If you need F3 combos (e.g., for eye measuring), remove F3 from the exclusion list.

      Example: If F3 is an exclusion key for your EyeZoom hotkey, pressing Shift+F3 will block the hotkey from activating.
    did_not_help: kb_other

  sol_kb_game_states:
    type: solution
    text: |
      Go to **Advanced -> Hotkeys -> [Your Hotkey] -> Required Game States**
      and set it to **"In World (Cursor Grabbed)"**.

      **Common mistake:** "Cursor Free" is the opposite of what you want. "Cursor Free" means
      the cursor is visible (inventory/menu). "Cursor Grabbed" means normal gameplay where the
      cursor controls the camera.

      **Important:** Game States require the **state-output mod** to be installed.
      Check that `wpstateout.txt` exists in your `.minecraft` folder. If you previously had state-output
      installed but removed it, delete the leftover `wpstateout.txt` file.

      **Trade-off:** Setting **"In World (Cursor Grabbed)"** means your hotkeys won't fire while paused or in the inventory — this is the accepted trade-off for speedrunning use. If you want hotkeys in the pause menu, you'll need to leave game state filtering off and accept they may fire while typing.

      **Note on F3 exclusion key:** By default, **F3 is *not* added as an exclusion key** on resize hotkeys. Some users choose to add it manually to prevent accidental resizes when pressing combinations like **F3+G** or **F3+A**. If you want this behavior, go to **Advanced → Hotkeys → [Hotkey] → Exclusion Keys** and add **F3**. If you want your hotkey to fire while holding F3, simply leave the exclusion list unchanged.
    did_not_help: kb_other

  sol_kb_number_keys:
    type: solution
    text: |
      **Don't use number keys (1-9) as mode hotkeys.** They conflict with Minecraft's pie chart input, which also uses numbers in the "In World (Unpaused)" game state.
      This conflict **cannot be resolved** using Game States because both pie chart and mode hotkeys share the same state.
      **Fix:** Rebind your mode hotkeys to non-number keys.
    did_not_help: kb_other

  sol_kb_jingle_conflict:
    type: solution
    text: |
      If your macros require multiple presses or you see modes cycling through unexpected states,
      **Jingle's resizing script** may be running alongside Toolscreen and fighting over window state.

      **Fix:**
      1. Open Jingle
      2. Go to **Scripts > Resizing > Disable**
      3. Or close Jingle entirely if you no longer need it

      Toolscreen replaces Jingle's resizing functionality. You do NOT need both running.
    did_not_help: kb_other

  kb_other:
    type: question
    text: "Which of these describes your issue?"
    options:
      - label: "Ctrl+I grabs cursor but settings menu doesn't appear (macros still work)"
        next: sol_kb_font_crash
      - label: "Ctrl+I only works when OBS is open"
        next: sol_kb_ctrli_obs
      - label: "Sensitivity hotkey doesn't hold (reverts only on mode change)"
        next: sol_kb_sens_intended
      - label: "Ctrl+I doesn't work at all (set a conflicting hotkey)"
        next: sol_kb_config_reset
      - label: "Rebound key triggers a Windows shortcut (Alt+Tab, Alt+Esc, etc.)"
        next: sol_kb_system_shortcut
      - label: "Something else"
        next: escalate_generic

  sol_kb_font_crash:
    type: solution
    text: |
      This is likely caused by a **corrupted font path** in your config.
      v1.0.9 partially fixed this (invalid fonts now fall back to Arial), but issues can still occur.

      **Fix:**
      1. Open `C:\Users\<username>\.config\toolscreen\config.toml`
      2. Find `textFontPath` and delete the entire line
      3. Save and restart Minecraft

      Alternatively, delete the entire `config.toml` to reset all settings.
      **Update Toolscreen** to the latest version for the best font handling.
    did_not_help: sol_kb_config_reset

  sol_kb_ctrli_obs:
    type: solution
    text: |
      This issue has two known causes:

      **Cause 1 (AMD GPU):** AMD Adrenalin / Radeon Software injects DLLs that interfere.
      Fix: Disable AMD Adrenalin overlay or close it entirely.

      **Cause 2 (Any GPU, hook chaining):** Toolscreen needs a secondary render path.
      Fixes (try in order):
      1. **Update to Toolscreen v1.1+** (hook chaining is now enabled by default)
      2. If still broken, try changing the hook chaining target in `config.toml`:
         - Find `hookChainingTarget` and try switching between `Original` and `Latest`
      3. If hook chaining causes crashes, set `disableHookChaining` to `true`
      4. If nothing works, enable **Virtual Camera** in Toolscreen Advanced settings

      **Workaround:** Keep OBS open with a Game Capture source for Minecraft (does not need to record).
    did_not_help: escalate_generic

  sol_kb_sens_intended:
    type: solution
    text: |
      This is **intended behavior**, not a bug. The non-toggle sensitivity hotkey changes sensitivity until the **next mode change**, not just while held.
      A "hold" option is a feature request that hasn't been implemented yet.
    did_not_help: escalate_generic

  sol_kb_config_reset:
    type: solution
    text: |
      You likely set a hotkey that conflicts with Ctrl+I. Settings are stored **externally** (not per-instance), so reinstalling won't help.
      Delete the config file to reset all settings:
      `C:\Users\<username>\.config\toolscreen\config.toml`
    did_not_help: escalate_generic

  sol_kb_system_shortcut:
    type: solution
    text: |
      Toolscreen rebinds only intercept keys **at the game level**, not the OS level.
      If you rebind to a key that's part of a Windows shortcut (Alt+Tab, Alt+Esc, Win+key, etc.),
      Windows will still process the shortcut before Toolscreen can remap it.

      **This is a fundamental limitation** -- Toolscreen cannot block OS-level shortcuts.

      **Fix:** Use **AutoHotKey (AHK)** instead for rebinds that involve system shortcut keys.
      AHK intercepts keys globally before the OS processes them.
    did_not_help: escalate_generic

  # ============================================================
  # BRANCH 6: Eye zoom / sensitivity issue
  # ============================================================
  sens_type:
    type: question
    text: "What is the specific issue?"
    options:
      - label: "Eye wiggle (ender eye fluctuates between readings)"
        next: sol_sens_eye_wiggle
      - label: "Sensitivity feels wrong or choppy / pixel skipping"
        next: sens_raw_input
      - label: "Pixel skipping at a specific multiplier value (jumpy movement)"
        next: sol_sens_pixel_skipping
      - label: "Sensitivity resets to default on every new world"
        next: sol_sens_resets_world
      - label: "Eye zoom looks very choppy / low FPS (2-5 FPS)"
        next: sol_sens_eyezoom_fps
      - label: "Zoom level too close or too far (compared to Jingle)"
        next: sol_sens_zoom_height
      - label: "Eye appears stretched in zoom overlay"
        next: sol_sens_stretched
      - label: "Game twitches/jitters when entering eye measuring mode"
        next: sol_sens_eyezoom_twitch
      - label: "Eye zoom becomes transparent/see-through when pausing with Esc"
        next: sol_sens_eyezoom_esc_opacity
      - label: "Eye zoom magnifier is flickering"
        next: sol_sens_magnifier_flicker
      - label: "Camera snaps to floor / spins when entering EyeZoom (raw input was off)"
        next: sol_sens_camera_snap
      - label: "EyeZoom projector disappeared / broken after adjusting width"
        next: sol_eyezoom_width_broken
      - label: "Boat eye setup / godsens / FOV / Enhanced Pointer Precision"
        next: sol_faq_boateye_setup

  sol_sens_eye_wiggle:
    type: solution
    text: |
      Eye wiggle is likely **not caused by Toolscreen**. Toolscreen doesn't change the angle per mouse input — it only affects input rate for a given mouse movement.

      Possible explanations:
      - Toolscreen's higher FPS rendering makes **existing wiggle more visible**
      - Your sensitivity settings may have changed during the tool switch
      - **Test it:** Do 30 throws with Jingle, then 30 with Toolscreen, and compare wiggle rates
    did_not_help: sens_raw_input

  sens_raw_input:
    type: question
    text: "Is **raw input** turned ON in Minecraft settings?"
    options:
      - label: "No / Not sure"
        next: sol_sens_enable_raw_input
      - label: "Yes, raw input is on"
        next: sens_godsens

  sol_sens_enable_raw_input:
    type: solution
    text: |
      Turn on **raw input** in Minecraft settings. Without it, the sensitivity slider has **no effect**, which means Toolscreen's sensitivity override also won't work.
    did_not_help: sens_godsens

  sens_godsens:
    type: question
    text: "Are you using **godsens** (the calculated optimal sensitivity value)?"
    options:
      - label: "No / What's godsens?"
        next: sol_sens_setup_godsens
      - label: "Yes, godsens is set up"
        next: sens_slider_max

  sol_sens_setup_godsens:
    type: solution
    text: |
      You need to use the Toolscreen sensitivity calculator to set up **godsens**:
      1. Go to: https://qmaxxen.github.io/Toolscreen-sens-calc/
      2. Enter your DPI and desired sensitivity
      3. Set the calculated value as your **in-game Minecraft sensitivity**
      4. Use Toolscreen's **Global Sensitivity** as a multiplier on top

      Using 100% in-game sensitivity causes choppy, pixel-skipping mouse movement.
    did_not_help: sens_slider_max

  sens_slider_max:
    type: question
    text: "Is the **Global Sensitivity** slider maxed out but you need a higher value?"
    options:
      - label: "Yes, the slider cap is too low"
        next: sol_sens_ctrl_click
      - label: "No, slider range is fine"
        next: escalate_generic

  sol_sens_ctrl_click:
    type: solution
    text: |
      **Ctrl-click** the Global Sensitivity input box to type in a value manually. This lets you exceed the slider's maximum (e.g., entering 5.08 when the slider only goes to 3.0).
    did_not_help: escalate_generic

  sol_sens_eyezoom_fps:
    type: solution
    text: |
      The EyeZoom FPS setting defaulted to a very low value. This was fixed in **v1.0.6**
      (the ZoomFPS option was removed entirely). **Update Toolscreen** to the latest version.
    did_not_help: sens_raw_input

  sol_sens_eyezoom_twitch:
    type: solution
    text: |
      Game twitching/jittering when entering eye measuring mode is a **known bug**.
      Try updating to the latest Toolscreen version. If it persists, this is an acknowledged issue awaiting a fix.
    did_not_help: escalate_generic

  sol_sens_zoom_height:
    type: solution
    text: |
      Jingle uses different default zoom settings. Adjust the **height** parameter in eyezoom mode settings.
      Try setting it to **3600-4000** for a similar zoom level to Jingle's default.
    did_not_help: escalate_generic

  sol_sens_stretched:
    type: solution
    text: |
      This is **intentional behavior** — the eye appears stretched because you're aiming at its center.
      **Adding more pixels** to the overlay reduces the perceived stretching. Adjust the pixel count in your eyezoom settings.
    did_not_help: escalate_generic

  sol_sens_eyezoom_esc_opacity:
    type: solution
    text: |
      This bug (EyeZoom overlay going transparent when pausing with Esc) was **fixed in v1.1.x**.
      **Update Toolscreen** to v1.1+ to resolve it permanently.

      **Workaround on older versions:** Use **F3+Esc** to pause instead of plain Esc. This activates the debug menu alongside the pause menu and does not trigger the transparency effect. Only the in-game view is affected — OBS and virtual camera captures are unaffected.
    did_not_help: escalate_generic

  sol_sens_magnifier_flicker:
    type: solution
    text: |
      The eye zoom magnifier sometimes flickers on the **F3+Esc** screen. Try:
      1. Use a different F3 state (toggle F3 off and back on)
      2. Update to the latest Toolscreen version
    did_not_help: escalate_generic

  sol_sens_camera_snap:
    type: solution
    text: |
      This was a **regression introduced in v1.1.1** that broke mouse delta handling when raw input was off, causing the camera to violently snap downward and spin when entering any resize mode (Thin, Wide, EyeZoom).

      **Fix:**
      1. **Always keep raw input ON** in Minecraft settings — this is required for Toolscreen to function correctly
      2. If you want a slower sensitivity in EyeZoom for measuring, use Toolscreen's **EyeZoom Sensitivity Override** slider (Ctrl+I -> Advanced -> Modes -> EyeZoom -> Sensitivity) instead of toggling raw input off
      3. **Update to v1.1.3+** which patches the underlying code bug

      You should never need to turn raw input off when using Toolscreen.
    did_not_help: escalate_generic

  sol_eyezoom_width_broken:
    type: solution
    text: |
      Setting the EyeZoom width **below a certain minimum** causes the projector to disappear and not recover, even after restarting or resetting to defaults.

      **Fix:**
      1. Restore your config from backup: `C:\Users\<username>\.config\toolscreen\backups\`
         Open the backups folder, find the most recent backup, and copy it over `config.toml`

      **Minimum width advisory:**
      - The **Thin mode** has a minimum of **330 px** to avoid cutting off the E counter and CPU name on the F3 screen
      - If you need a narrower view, create a **custom mode** with no minimum: **Advanced -> Modes -> (+ add custom mode)**
      - The **EyeZoom height** should remain at **16384** — changing it breaks NinjaBrainBot's angle calculation (it assumes a specific pixel-per-degree ratio at that height). Width can be adjusted freely (384 is standard)
    did_not_help: escalate_generic

  # ============================================================
  # BRANCH 7A: NinjaBrainBot overlay (setup + issues)
  # ============================================================
  ninb_setup_or_issue:
    type: question
    text: "What do you need help with?"
    options:
      - label: "How to set up the NinjaBrainBot overlay"
        next: ninb_setup_type
      - label: "Overlay is laggy / slow to update"
        next: perf_overlay_image_vs_window
      - label: "Overlay doesn't appear / is invisible"
        next: sol_ninb_not_visible
      - label: "Overlay not updating coordinates after F3+C"
        next: ov_ninb_minimized
      - label: "How to show/hide overlay per mode or hotkey"
        next: sol_ninb_toggle
      - label: "How to move/reposition the overlay"
        next: sol_ninb_position
      - label: "How to make overlay background transparent"
        next: sol_ninb_color_key
      - label: "Overlay disappears after ~30 seconds"
        next: sol_ninb_auto_hide
      - label: "NinjaBrainBot angle changed after Toolscreen install"
        next: sol_ninb_angle_change
      - label: "Pressing NinjaBrainBot hotkey tabs out of the game"
        next: sol_ninb_hotkey_tabout
      - label: "Two overlays showing at once (one ghost/translucent)"
        next: sol_ninb_dual_overlay
      - label: "Overlay disappears / not visible in EyeZoom mode"
        next: sol_ninb_eyezoom_missing
      - label: "Overlay squishes or distorts when NinB window changes size"
        next: sol_ninb_squish
      - label: "NinjaBrainBot hotkeys not working (running as admin)"
        next: sol_ninb_admin

  ninb_setup_type:
    type: question
    text: "Which overlay type do you want to use?"
    options:
      - label: "Window overlay (recommended — real-time updates)"
        next: sol_ninb_setup_window
      - label: "Image overlay (simpler but has 1-sec delay)"
        next: sol_ninb_setup_image
      - label: "Not sure which to pick"
        next: sol_ninb_setup_window

  sol_ninb_setup_window:
    type: solution
    text: |
      To set up the **window overlay** (recommended):
      1. Open NinjaBrainBot and keep it running (do NOT minimize it)
      2. In Toolscreen, go to **Advanced -> Overlays**
      3. Add a new overlay, select **Window Overlay**
      4. Select the NinjaBrainBot window
      5. On a single monitor, use **Win+Tab** to move NinjaBrainBot to a **separate virtual desktop** so it stays running without covering your game
      6. Disable NinjaBrainBot's **"bring to front on click"** setting in NinjaBrainBot's preferences
      7. **Add the overlay to each mode you want it in:**
         Go to Advanced > Modes > Fullscreen > Window Overlays > Add it.
         Repeat for EyeZoom, Thin, Wide, etc. The overlay won't appear in modes where it hasn't been added.

      **Known limitation:** Window overlays can sometimes "capture" mouse input when you hover over
      the area where NinjaBrainBot is positioned (even if it's on another virtual desktop). If this
      causes pauses or tab-outs, try repositioning the NinjaBrainBot window on the virtual desktop
      to a different location, or switch to the **image overlay** method.

      **If your hotkey isn't making NinBot disappear:** Go to Modes > Fullscreen > Window Overlays
      and remove the NinBot overlay from there. It should only be in the modes where you want it visible.

      Also, do NOT click the NinjaBrainBot minimize button while using window overlay -- it will
      cause the overlay to freeze. If the overlay freezes, restart Minecraft.
    did_not_help: sol_ninb_not_visible

  sol_ninb_setup_image:
    type: solution
    text: |
      To set up the **image overlay**:
      1. In NinjaBrainBot, enable the overlay image output
      2. In Toolscreen, go to **Advanced -> Overlays**
      3. Add a new overlay, select **Image Overlay**
      4. Point it to the NinjaBrainBot overlay image file

      Note: The image overlay has a **hardcoded 1-second update delay**. For real-time updates, use the window overlay instead.
    did_not_help: sol_ninb_not_visible

  sol_ninb_not_visible:
    type: solution
    text: |
      If the NinjaBrainBot overlay doesn't appear:
      1. Make sure NinjaBrainBot is actually **running** (not just installed)
      2. Check that the overlay is **enabled** in Toolscreen's overlay settings
      3. Make sure there isn't a **hotkey for disabling mirrors/overlays** that's accidentally set — check Advanced -> Hotkeys
      4. Try switching between image and window overlay types
    did_not_help: escalate_generic

  sol_ninb_toggle:
    type: solution
    text: |
      You can show/hide the NinjaBrainBot overlay per mode:
      1. Go to **Advanced -> Modes -> [Your Mode] -> Overlays**
      2. Enable or disable the NinjaBrainBot overlay for each specific mode
      3. Example: Enable it only in your EyeZoom mode, disable it in Fullscreen mode

      You can also set a **hotkey to toggle overlay visibility** in Advanced -> Hotkeys.
    did_not_help: escalate_generic

  sol_ninb_position:
    type: solution
    text: |
      To move/reposition the NinjaBrainBot overlay:
      1. Open the Toolscreen config menu (Ctrl+I)
      2. Go to **Advanced -> Overlays -> [Your NinB Overlay]**
      3. Adjust the **X/Y position** and **size** values
      4. You can set **different positions per mode** if needed
      5. **Important:** If the overlay disappears when switching to EyeZoom mode, change the
         position anchor from **"Bottom Right (Viewport)"** to **"Bottom Right (Screen)"**.
         The viewport changes size when entering EyeZoom, which moves viewport-anchored
         overlays off-screen.
    did_not_help: escalate_generic

  sol_ninb_color_key:
    type: solution
    text: |
      To make the NinjaBrainBot overlay background transparent in-game:
      1. Go to **Advanced -> Overlays -> [Your NinB Window Overlay]**
      2. Scroll down and enable **Color Key**
      3. Click on the color picker and select (or paste) the hex code of NinjaBrainBot's background color
      4. The matching color will become transparent in the overlay
    did_not_help: escalate_generic

  sol_ninb_auto_hide:
    type: solution
    text: |
      NinjaBrainBot has an **auto-hide timer** (default ~25-30 seconds) that hides the OBS overlay after a period of inactivity.
      This makes the Toolscreen overlay disappear too.

      **Fix:** In **NinjaBrainBot's own settings**:
      1. Find the auto-hide / OBS overlay timer setting
      2. Either **uncheck** auto-hide, or set it to a very long duration
      3. Alternatively, switch to a **window overlay** in Toolscreen (not affected by NinB's auto-hide)
    did_not_help: escalate_generic

  sol_ninb_angle_change:
    type: solution
    text: |
      When using Toolscreen with NinjaBrainBot for boat eye, check these settings:

      **In NinjaBrainBot:**
      1. **Crosshair correction** must be **0** (non-zero values from Jingle tutorials will break it)
      2. **Tall resolution** should match your setup
      3. **Tall height** should be **16384**
      4. **Boat standard deviation** should be **0.0007**
      5. Sensitivity must match your `options.txt` / `standardsettings.json`
      6. Sensitivity should NOT be the default `0.012727`

      Use **/nbbdebug** in the NinjaBrainBot Discord to verify all settings.

      Set up godsens using the calculator: https://qmaxxen.github.io/Toolscreen-sens-calc/
    did_not_help: escalate_generic

  sol_ninb_hotkey_tabout:
    type: solution
    text: |
      NinjaBrainBot's native **"Toggle Visibility"** hotkey brings the NinjaBrainBot window into focus when pressed, which tabs you out of Minecraft.

      **Fix:**
      1. In **NinjaBrainBot settings**, find the "Toggle Visibility" hotkey and **disable it** (clear the bind)
      2. Instead, use Toolscreen's own hotkey: **Advanced -> Hotkeys -> Toggle Window Overlays**
         This hides/shows the overlay inside the game without switching focus

      You should not use NinjaBrainBot's native hotkey at all when running it through Toolscreen's window overlay.
    did_not_help: escalate_generic

  sol_ninb_dual_overlay:
    type: solution
    text: |
      You have **both** the image overlay and the window overlay active simultaneously, causing a translucent ghost image behind the sharp window overlay.

      **Fix:**
      1. Go to **Ctrl+I -> Basic**
      2. Find **"Enable NinjaBrainBot Overlay"** and set it to **Off**
      3. Keep only the **window overlay** active (set up via Advanced -> Overlays)

      The Basic tab's NinjaBrainBot overlay toggle controls the image overlay. If you've switched to using the window overlay method, you must disable the image overlay here.
    did_not_help: escalate_generic

  sol_ninb_admin:
    type: solution
    text: |
      If Minecraft (or your launcher) is **run as administrator**, NinjaBrainBot cannot send keystrokes to it unless NinjaBrainBot is also run as administrator. Windows restricts inter-process keyboard injection between different privilege levels.

      **Fix:** Do **not** run your Minecraft launcher as administrator.
      Right-click the launcher icon -> Properties -> Compatibility tab -> uncheck "Run this program as an administrator".

      If you must run the launcher as admin for some reason, you'd also need to run NinjaBrainBot as admin — but this is not recommended.
    did_not_help: escalate_generic

  # ============================================================
  # BRANCH 7B: Pie mirror / other overlay issues
  # ============================================================
  ov_pie_or_other:
    type: question
    text: "Which overlay has the issue?"
    options:
      - label: "Pie mirror"
        next: ov_pie_issue
      - label: "Unspecified values not showing in thin mode"
        next: sol_ov_color_code
      - label: "Spikes look different size in Preemptive vs EyeZoom mode"
        next: sol_mode_preemptive_vs_eyezoom
      - label: "Other overlay"
        next: escalate_generic

  ov_ninb_minimized:
    type: question
    text: "Is NinjaBrainBot **minimized** or hidden behind the Minecraft window?"
    options:
      - label: "Yes / Maybe"
        next: sol_ov_ninb_virtual_desktop
      - label: "No, it's visible"
        next: sol_ov_ninb_bring_to_front

  sol_ov_ninb_virtual_desktop:
    type: solution
    text: |
      When NinjaBrainBot is minimized, the window overlay **stops updating**.
      Instead of minimizing, use **Win+Tab** to move NinjaBrainBot to a **separate virtual desktop**.
      This keeps it "running" and visible to Toolscreen without covering your game.

      Also check: in newer versions of Toolscreen, there is a **"Force Update"** checkbox in the Window Overlay settings — enabling this can help if the overlay stops refreshing even when NinjaBrainBot is visible on another virtual desktop.
    did_not_help: sol_ov_ninb_bring_to_front

  sol_ov_ninb_bring_to_front:
    type: solution
    text: |
      Disable NinjaBrainBot's **"bring to front on click"** setting (in NinjaBrainBot's own settings).
      On a single monitor, this setting can cause focus conflicts with Minecraft.

      Alternative: Use an **AutoHotKey script** to toggle focus between NinjaBrainBot and Minecraft with a hotkey.
    did_not_help: escalate_generic

  ov_pie_issue:
    type: question
    text: "What's wrong with the pie mirror?"
    options:
      - label: "Pie mirror shows nothing at all"
        next: sol_ov_pie_directory
      - label: "Numbers show but colors are wrong"
        next: sol_ov_pie_color
      - label: "Mob spawner numbers not showing"
        next: sol_ov_pie_mobspawner
      - label: "Pie chart in wrong position / can't move it"
        next: sol_ov_pie_position
      - label: "Mirror accidentally deleted / disappeared"
        next: sol_mirror_deleted
      - label: "Mirror shows CPU name instead of entity count"
        next: sol_mirror_cpu_bleed

  sol_ov_pie_directory:
    type: solution
    text: |
      Check that the **pie directory** is set correctly in your pie mirror settings.
      It should point to the correct folder where Minecraft outputs pie chart data.
    did_not_help: escalate_generic

  sol_ov_pie_color:
    type: solution
    text: |
      In your mirror settings:
      1. **Uncheck "color passthrough"** — this lets you change the output color
      2. **Increase the "color sensitivity"** number if values aren't being detected
    did_not_help: escalate_generic

  sol_ov_pie_mobspawner:
    type: solution
    text: |
      To see mob spawner numbers on pie:
      1. Create a **new mirror**
      2. Set mode to: **fullscreen -> mirrors -> mobspawner**
      3. **Uncheck "color passthrough"**
      4. Increase the **"color sensitivity"** number if needed
    did_not_help: escalate_generic

  sol_ov_pie_position:
    type: solution
    text: |
      The pie chart position is controlled by the **mirror group**, not the individual mirror.
      Go to **Advanced -> Modes -> [Your Mode] -> Mirror Groups -> [Preemptive Pie group]**
      and change the **output position** there.
    did_not_help: escalate_generic

  sol_thin_pie_doubled:
    type: solution
    text: |
      If you see what looks like a **second pie chart** below the real one in thin mode,
      your thin width is **too narrow**.

      **Fix:** Increase your thin mode width. For example, change it from 295 to **320 or higher**.
      Go to **Advanced -> Modes -> Thin -> Width** and increase the value.
    did_not_help: escalate_generic

  sol_ov_color_code:
    type: solution
    text: |
      This is a **GPU-specific** issue. Certain GPUs render color code values slightly differently, causing detection to fail.
      Try using **adjacent color code values** (e.g., 203 instead of 204) until you find one your GPU reads correctly.
    did_not_help: escalate_generic

  # ============================================================
  # BRANCH 8: OBS capture issue
  # ============================================================
  obs_symptom:
    type: question
    text: "What is the issue?"
    options:
      - label: "Black screen in OBS"
        next: obs_capture_type
      - label: "White screen in OBS (EyeZoom area is white)"
        next: eyezoom_white_version
      - label: "Animations / transitions not showing in OBS"
        next: obs_anim_borderless
      - label: "Game appears upside down in OBS"
        next: sol_obs_upside_down
      - label: "Discord screen share / Medal / non-OBS recording broken"
        next: sol_obs_discord_screenshare
      - label: "How to set up Virtual Camera for Discord"
        next: sol_obs_virtual_camera_setup
      - label: "Toolscreen features only work when OBS is open"
        next: sol_obs_dependency
      - label: "Virtual camera / FPS drop from virtual camera"
        next: sol_obs_virtual_camera_fps

  # --- OBS: Game Capture vs Window Capture confusion ---
  obs_capture_type:
    type: question
    text: "Are you using a **Game Capture** source or a **Window Capture** source in OBS?"
    options:
      - label: "Game Capture"
        next: sol_obs_black_screen
      - label: "Window Capture"
        next: sol_obs_use_game_capture
      - label: "Not sure"
        next: sol_obs_use_game_capture

  sol_obs_use_game_capture:
    type: solution
    text: |
      You need to use a **Game Capture** source, not a Window Capture.
      Some users create a Window Capture and name it "Game Capture" — this is not the same thing.

      1. In OBS, **remove** your current capture source
      2. Click **+** to add a new source, select **Game Capture**
      3. Set it to capture the window labeled **"Minecraft*"**

      Note: OOMS (On-screen Mode Switching) only works with **Game Capture** or **Virtual Camera**, not Window Capture.
    did_not_help: sol_obs_black_screen

  # --- OBS: Virtual Camera setup ---
  sol_obs_virtual_camera_setup:
    type: solution
    text: |
      To set up **Virtual Camera** for Discord screen sharing:
      1. **Install OBS** (you need the OBS Virtual Camera driver, even if you won't use OBS itself)
      2. Install Toolscreen
      3. Launch Minecraft with Toolscreen
      4. In Toolscreen: **Advanced -> Settings -> Virtual Camera** -> Enable
      5. In Discord: use the **camera button** (not screen share) and select **OBS Virtual Camera**

      **Important:**
      - Do NOT enable Virtual Camera inside OBS — Toolscreen takes over the driver directly
      - OBS just needs to be **installed** for the driver. OBS can be closed
      - Don't use Virtual Camera unless you need it (for Discord screenshare or if Game Capture fails)
      - The preview may appear upside-down on your end — this is a Discord bug, viewers see it correctly
    did_not_help: escalate_generic

  sol_obs_black_screen:
    type: solution
    text: |
      After installing Toolscreen, your OBS capture source needs to be updated.
      1. Change your Game Capture to target the window labeled **"Minecraft*"** (the window Toolscreen creates), not the old instance-specific capture
      2. If it's still a black screen, open your Game Capture source properties and enable **"SLI/Crossfire Capture Mode (Slow)"**
    did_not_help: escalate_generic

  obs_anim_borderless:
    type: solution
    text: |
      First, make sure **viewport animations** are set to **Cut** (not Slide/Fade) on **all modes** including Fullscreen:
      - Ctrl+I > **Advanced > Modes > [Each Mode] > Viewport Animation Type: Cut**

      If animations still don't show in OBS after setting Cut, what display mode are you using?
    did_not_help: obs_anim_display_mode

  obs_anim_display_mode:
    type: question
    text: "Are you using **borderless** or **fullscreen** mode?"
    options:
      - label: "Borderless"
        next: sol_obs_anim_borderless
      - label: "Fullscreen"
        next: sol_obs_anim_fullscreen

  sol_obs_anim_borderless:
    type: solution
    text: |
      Known issue with **Game Capture in borderless** mode. Try:
      1. Use **Virtual Camera** capture instead of Game Capture
      2. Or switch to **fullscreen** and Alt-Tab out/in to force OBS to re-acquire the game window
    did_not_help: escalate_generic

  sol_obs_anim_fullscreen:
    type: solution
    text: |
      Check that **"Show 3rd party overlays"** is enabled in your OBS Game Capture source settings.
      Also try: Alt-Tab out of Minecraft and back in to force OBS to re-acquire the game window.
    did_not_help: escalate_generic

  sol_obs_upside_down:
    type: solution
    text: |
      Switch your capture method. Try cycling between:
      - **Game Capture** <-> **Window Capture** <-> **Virtual Camera**
      One of these should render correctly for your setup.
    did_not_help: escalate_generic

  sol_obs_discord_screenshare:
    type: solution
    text: |
      Discord screen share and other non-OBS recording tools (Medal, etc.) often don't work in fullscreen because they can't hook into the game the same way OBS does.

      **Workarounds:**
      1. Enable **Virtual Camera** in Toolscreen: **Advanced -> Settings -> Virtual Camera**
         - Then in Discord, use the **camera button** (not screen share) to share the virtual camera output
         - Note: the preview may appear upside-down on your end. This is a Discord bug; viewers see it correctly.
      2. Use OBS with **Game Capture** source, then share OBS's output via Discord/Medal
      3. Or use **borderless** mode instead of fullscreen (may have other trade-offs)
    did_not_help: escalate_generic

  sol_obs_dependency:
    type: solution
    text: |
      This is usually caused by **AMD Adrenalin / Radeon Software** injecting graphics DLLs that Toolscreen depends on. OBS's capture hook changes the DLL loading behavior, making Toolscreen work.

      **Try these in order:**
      1. If you have an **AMD GPU**: disable the **AMD Adrenalin overlay** or close AMD Radeon Software entirely
      2. If that doesn't help or isn't applicable: keep OBS running in the background with a Game Capture source for Minecraft (it doesn't need to be recording)
    did_not_help: escalate_generic

  # ============================================================
  # BRANCH 9: Settings not saving
  # ============================================================
  save_which:
    type: question
    text: "Which settings aren't saving?"
    options:
      - label: "Thin/wide resolution settings"
        next: sol_save_resolution
      - label: "Theme / preset colors"
        next: sol_save_themes
      - label: "All settings reset on every restart"
        next: save_all_reset
      - label: "Can't open config menu (Ctrl+I broken)"
        next: sol_kb_config_reset

  sol_save_resolution:
    type: solution
    text: |
      This is a **known open bug** — resolution set in the **Basic tab** does not save reliably.

      **Workaround:** Use the **Advanced tab** to set your thin/wide resolution instead.
      Go to **Ctrl+I -> Advanced -> Modes -> Thin/Wide -> Resolution** and set the value there.
      Settings set via the Advanced tab do persist correctly.
    did_not_help: escalate_generic

  sol_save_themes:
    type: solution
    text: |
      This was a bug fixed in **v1.0.4**. **Update Toolscreen** to the latest version — custom theme colours now save correctly.
    did_not_help: escalate_generic

  save_all_reset:
    type: question
    text: "Does your **Windows username** contain non-English characters (accents, special symbols)?"
    options:
      - label: "Yes"
        next: sol_save_unicode
      - label: "No"
        next: sol_save_av_quarantine

  sol_save_unicode:
    type: solution
    text: |
      Non-English characters in your username prevent the config file from saving correctly.
      **Update to the latest Toolscreen version** which includes Unicode path support.
    did_not_help: sol_save_av_quarantine

  sol_save_av_quarantine:
    type: solution
    text: |
      Your antivirus may be **deleting or quarantining** the config file after each session.
      1. Check if the config file exists after closing: `C:\Users\<username>\.config\toolscreen\config.toml`
      2. Add the entire `.config\toolscreen\` folder to your antivirus exclusions
      3. If using a third-party AV, consider uninstalling it (see Branch 1 for details)
    did_not_help: escalate_generic

  # ============================================================
  # BRANCH 10: Feature requests (informational)
  # ============================================================
  # These are handled at the root level by the "Something else" option.
  # The escalate_generic node covers unknown issues and feature requests.

  # ============================================================
  # BRANCH 11: How to update / uninstall
  # ============================================================
  howto_update_or_uninstall:
    type: question
    text: "What do you need to do?"
    options:
      - label: "How to update Toolscreen"
        next: sol_howto_update
      - label: "Auto-update ran but version didn't change (silent fail)"
        next: sol_autoupdate_silent_fail
      - label: "How to uninstall Toolscreen"
        next: sol_howto_uninstall
      - label: "How to copy settings between instances"
        next: sol_config_universal

  sol_howto_update:
    type: solution
    text: |
      **Since v1.0.6**, Toolscreen has an **auto-updater** that prompts on launch.

      To update manually:
      1. Download the **new .jar file** from Toolscreen releases
      2. Place it in the same folder as the old one
      3. **Double-click** the new .jar to install it
      4. Delete the old .jar file

      Settings are preserved automatically across updates.
    did_not_help: escalate_generic

  sol_howto_uninstall:
    type: solution
    text: |
      **Quickest method:** Double-click the Toolscreen .jar file and select **Uninstall**.

      To manually uninstall:
      1. Remove the Toolscreen .jar from your instance
      2. Remove the **pre-launch command** from your launcher's instance settings
      3. Delete the config folder: `C:\Users\<username>\.config\toolscreen\`

      If your launcher won't start because it expects the pre-launch command, clear it in instance settings.
    did_not_help: escalate_generic

  sol_config_universal:
    type: solution
    text: |
      Toolscreen settings are **universal** by default:
      Config file: `C:\Users\<username>\.config\toolscreen\config.toml`

      All your instances share the same settings automatically.

      For **per-instance settings**, create a `toolscreen` folder inside your instance's `.minecraft/` directory.
      Toolscreen will create a `config.toml` there that takes priority over the global config.
    did_not_help: escalate_generic

  # ============================================================
  # BRANCH 12: Resolution mismatch
  # ============================================================
  sol_resolution_mismatch:
    type: solution
    text: |
      Toolscreen defaults to 1080p. On non-1080p monitors (1440p, 4K, ultrawide), you need
      to set the resolution manually.

      Go to **Ctrl+I -> Advanced -> Modes -> Fullscreen** and set the resolution to match
      your monitor (e.g., 2560x1440 for 1440p, 3840x2160 for 4K).
    did_not_help: escalate_generic

  # ============================================================
  # SHARED: Universal escalation
  # ============================================================
  escalate_generic:
    type: escalate
    text: |
      Let's collect some information so the dev team can investigate.
      Before we start, try these **universal fixes** first:
      1. Update Toolscreen to the latest version
      2. Delete config: `C:\Users\<username>\.config\toolscreen\config.toml`
      3. Add the config folder to Windows Defender exclusions
      4. Close MSI Afterburner / RivaTuner / other overlay software
      5. Update GPU drivers

      If none of those helped, please provide the following:
    collect:
      - key: os
        prompt: "What OS and version? (e.g., Windows 11 23H2)"
      - key: ts_version
        prompt: "What Toolscreen version? (e.g., 1.0.8)"
      - key: mc_version
        prompt: "What Minecraft version?"
      - key: launcher
        prompt: "What launcher + version? (e.g., Prism 8.4)"
      - key: java
        prompt: "What Java version? (must be 17+)"
      - key: gpu
        prompt: "What GPU? (e.g., Intel HD 4600, RTX 3060)"
      - key: display_mode
        prompt: "Display mode? (Fullscreen / Borderless / Windowed)"
      - key: log
        prompt: "Please upload your Toolscreen log: `C:\\Users\\<username>\\.config\\toolscreen\\logs\\latest.log`"
      - key: launcher_log
        prompt: "Please upload your launcher log (from Prism/MultiMC log viewer)"
      - key: steps
        prompt: "Describe the issue and steps to reproduce it"

  # ============================================================
  # BRANCH 13: EyeZoom / clone all white
  # ============================================================
  eyezoom_white_version:
    type: question
    text: "What Toolscreen version are you on?"
    options:
      - label: "1.1.1 or 1.1.2"
        next: sol_eyezoom_white_1112
      - label: "1.1.3"
        next: sol_eyezoom_white_113
      - label: "1.1.4 or newer"
        next: sol_eyezoom_white_114plus
      - label: "Not sure"
        next: sol_eyezoom_white_1112

  sol_eyezoom_white_1112:
    type: solution
    text: |
      The **all-white EyeZoom overlay** is a known bug in **v1.1.1 and v1.1.2**. It occurs when Toolscreen calibrates to the wrong OpenGL texture, often due to OBS, Medal, or RivaTuner hooking in first.

      **Immediate fixes (try in order):**
      1. **Update to v1.1.4** — this is the most complete fix
      2. Close **OBS**, **Medal**, and **RivaTuner** before launching Minecraft, then restart
      3. If you need OBS open: in your OBS Game Capture source properties, enable **"Capture third-party overlays"**
      4. Restart Minecraft until the overlay loads correctly (may take 5+ attempts in rare cases)

      **Permanent workaround if you can't update:** Downgrade to **v1.0.9**.
    did_not_help: escalate_generic

  sol_eyezoom_white_113:
    type: solution
    text: |
      On **v1.1.3**, the white-screen bug was partially fixed. A side effect is that **OBS Game Capture** now requires **"Capture third-party overlays"** to be enabled to see the Toolscreen overlay.

      **Fix:**
      1. In OBS, right-click your **Game Capture** source → **Properties**
      2. Enable **"Capture third-party overlays"**
      3. If the overlay is still white, close **Medal** entirely (it masquerades as the OBS hook and confuses Toolscreen's texture lookup)

      For a complete fix, **update to v1.1.4** where this option is no longer required.
    did_not_help: escalate_generic

  sol_eyezoom_white_114plus:
    type: solution
    text: |
      The white EyeZoom overlay bug was fully resolved in **v1.1.4**. If you're still seeing a white overlay on v1.1.4+:

      1. Close **Medal** entirely — it can still interfere on some configurations
      2. Disable **AMD Adrenalin overlay** if you have an AMD GPU
      3. Delete `config.toml` and let it regenerate
      4. If nothing else works, close all overlay software (OBS, Medal, RTSS) and test with a clean launch
    did_not_help: escalate_generic

  # ============================================================
  # BRANCH 14: Mode stuck as default
  # ============================================================
  sol_mode_stuck_default:
    type: solution
    text: |
      You've accidentally set an overlay mode (EyeZoom, Thin, Wide, etc.) as your **default mode** instead of Fullscreen.

      **Fix:**
      1. Press **Ctrl+I** to open the settings menu
      2. Go to **Advanced -> Modes -> Fullscreen**
      3. Click **"Set as Default Mode"**

      Toolscreen will now always start in fullscreen and only switch to other modes when you press the relevant hotkey.
    did_not_help: escalate_generic

  # ============================================================
  # BRANCH 15: Pie directory / StandardSettings
  # ============================================================
  sol_pie_directory_standardsettings:
    type: solution
    text: |
      Minecraft resets the pie chart directory every time a new world is loaded. You need to lock it via the **StandardSettings** mod.

      **Fix:**
      1. Open `.minecraft/config/mcsr/standardsettings.json`
      2. Add or update the `pieDirectory` field:
         - For **mapless / preemptive**: `"pieDirectory": "root.gameRenderer.level.entities"`
         - For **fortress nav**: `"pieDirectory": "root.tick.level.entities.blockEntities"`

      **Common mistake — `rootroot`:** If the field already starts with `root.` and you type `root.` again, you'll get `rootroot.gameRenderer...` which Minecraft doesn't recognise and no pie will appear. Make sure the value starts with exactly one `root.`.

      Close the game before editing the file.
    did_not_help: escalate_generic

  # ============================================================
  # BRANCH 16: Linux
  # ============================================================
  sol_linux_waywall:
    type: solution
    text: |
      **Toolscreen is Windows-only** and does not run on Linux.

      Linux users should use **Waywall** instead, which provides equivalent functionality:
      https://github.com/tesselslate/waywall
    did_not_help: escalate_generic

  # ============================================================
  # BRANCH 17: Custom cursor / background image path
  # ============================================================
  sol_cursor_unicode_path:
    type: solution
    text: |
      If your custom cursor or background image file path contains **non-ASCII characters** (e.g., accented letters, non-Latin characters, or symbols), Toolscreen will fail to load the file with an error like `invalid image: can't fopen`.

      **Fix:** Move the image file to a path with only standard English characters.
      For example:
      - `C:\toolscreen_images\mycursor.png` ✓
      - `C:\Users\André\Pictures\cursor.png` ✗ (accented character in path)

      After moving the file, update the path in Toolscreen settings.
    did_not_help: escalate_generic

  # ============================================================
  # §1.4  JARfix
  # ============================================================
  sol_jarfix:
    type: solution
    text: |
      If double-clicking the Toolscreen `.jar` does nothing, or Windows tries to open it as a ZIP/archive, your system's `.jar` file association is broken — Java is not set as the default handler.

      **Fix:** Download and run **JARfix**: https://github.com/qMaxXen/Jarfix/releases/latest
      It resets the `.jar` association to Java in one click.

      After running JARfix, try double-clicking the Toolscreen `.jar` again.
    did_not_help: nsu_antivirus

  # ============================================================
  # §3.2  Borderless popup removed in 1.1.x
  # ============================================================
  sol_borderless_popup_gone:
    type: solution
    text: |
      The **"Click here to go borderless"** banner that appeared in the top-left was removed in **v1.1.x**. To go borderless now, use any of these methods:

      1. **Ctrl+I → Basic → "Go Borderless"** button
      2. Enable **Auto Borderless**: Ctrl+I → Basic → Other → Auto Borderless (goes borderless automatically on launch)
      3. Set a hotkey: Ctrl+I → Advanced → Hotkeys → Toggle Borderless
    did_not_help: escalate_generic

  # ============================================================
  # §3.3  FPS drop in pseudo-windowed / snapped state
  # ============================================================
  sol_perf_pseudowindowed:
    type: solution
    text: |
      If you're seeing severe FPS drops (e.g., 60 → 15 fps) while apparently in borderless mode, you may be in a **pseudo-windowed state** — for example, a window snapped to the top of the screen via Windows' snap feature, which looks fullscreen but isn't.

      Check the resolution shown in the F3 screen. If it's something like **1902×1033** instead of your monitor's native resolution, you're not in true borderless.

      **Fix:**
      1. Move the window back to a normal windowed state first (drag it away from the top edge)
      2. Then press your Toolscreen borderless hotkey to enter true borderless
      3. Do **not** use Windows snap to "maximise" the window — use Toolscreen's own borderless toggle
    did_not_help: escalate_generic

  # ============================================================
  # §7.3  Pixel skipping at specific multiplier values
  # ============================================================
  sol_sens_pixel_skipping:
    type: solution
    text: |
      If mouse movement feels unusually jumpy at a specific sensitivity multiplier (e.g., skipping 4–8 pixels per tick instead of ~1–2), this is a suspected **floating-point accumulation bug** in Toolscreen.

      Small sub-pixel movements are buffered internally and occasionally fire as a double-jump.

      **Workaround:** Adjust your multiplier slightly — even a small change like **1.35 → 1.351** can eliminate the skipping. Try values a few thousandths either side until it feels smooth.

      This is under investigation; no definitive fix exists yet beyond tweaking the value.
    did_not_help: escalate_generic

  # ============================================================
  # §7.4  Sensitivity resets on new world (StandardSettings)
  # ============================================================
  sol_sens_resets_world:
    type: solution
    text: |
      If your in-game sensitivity reverts to a different value every time you load a new world, the **StandardSettings mod** is overwriting it.

      **Fix:**
      1. Open `.minecraft/config/mcsr/standardsettings.json`
      2. Find the `mouseSensitivity` field and set it to your desired value
      3. Your Minecraft `options.txt` and `standardsettings.json` **must have the same value** — whichever is in standardsettings wins on world load
      4. Close the game before editing either file

      For godsens, the correct value is `0.02291165` in both files (not `0.012727`, which is the old default).
    did_not_help: escalate_generic

  # ============================================================
  # §8.1  NinB Force Update checkbox
  # ============================================================
  # (Already covered by sol_ov_ninb_virtual_desktop — adding "Force Update" note there via update below)

  # ============================================================
  # §8.2  NinB overlay squishes / drifts
  # ============================================================
  sol_ninb_squish:
    type: solution
    text: |
      The window overlay captures and scales NinjaBrainBot's window to a fixed output size. If NinjaBrainBot's window **changes height** (e.g., more text appears as you get a reading), the overlay squishes or stretches to fit.

      **Workarounds:**
      1. Resize NinjaBrainBot's window to a fixed size that accommodates the most text it ever shows, then don't resize it
      2. Use a **colour key** to make the background transparent — this hides the squishing visually
      3. Independent X/Y scaling for overlays is a **requested feature** not yet implemented

      The image overlay has the same squishing issue since it also captures a fixed region.
    did_not_help: escalate_generic

  # ============================================================
  # §8.4  NinB overlay not showing in EyeZoom mode
  # ============================================================
  sol_ninb_eyezoom_missing:
    type: solution
    text: |
      If the NinjaBrainBot overlay appears in fullscreen mode but **disappears in EyeZoom mode**, you need to add it to the EyeZoom mode separately — overlays must be added to each mode individually.

      **Fix:**
      1. Ctrl+I → **Advanced → Modes → EyeZoom → Window Overlays**
      2. Add your NinjaBrainBot overlay here

      **Important — use Screen anchor, not Viewport anchor:**
      EyeZoom mode has a **16384-pixel-tall viewport**. If your overlay position uses a Viewport anchor (e.g., "Bottom Right (Viewport)"), it will be placed relative to that giant viewport and appear off-screen.
      Change the anchor to **"(Screen)"** instead — e.g., "Bottom Right (Screen)".
    did_not_help: escalate_generic

  # ============================================================
  # §9.1  Caps Lock / Windows key can't be rebound
  # ============================================================
  sol_kb_capslock_winkey:
    type: solution
    text: |
      **Caps Lock** and the **Windows key** cannot be rebound by Toolscreen. These keys are intercepted at the OS level before they reach any application, so Toolscreen has no opportunity to remap them.

      **Fix:** Use **AutoHotKey (AHK)** to remap Caps Lock or the Windows key at the OS level:
      - Example AHK line to use Caps Lock as a key: `CapsLock:: Send, {F14}` (then bind F14 in Toolscreen)
      - AHK intercepts globally before Windows processes the key

      Toolscreen handles all other keys correctly; only these two require AHK.
    did_not_help: escalate_generic

  # ============================================================
  # §9.4  Backspace clears bind instead of assigning it
  # ============================================================
  sol_kb_backspace_bind:
    type: solution
    text: |
      Pressing Backspace while setting a hotkey **clears the bind** rather than assigning Backspace as the trigger. This is a known limitation in v1.0.x.

      **Workaround (v1.1+):**
      Use the **keyboard layout editor's "Custom"** input and manually enter keycode **8** (the Windows virtual key code for Backspace).
      Ctrl+I → Advanced → Inputs → Custom → type `8`.

      Alternatively, remap a different physical key to act as Backspace via the rebind system, then set that key as your hotkey trigger.
    did_not_help: escalate_generic

  # ============================================================
  # §9.5  "Types" field shows wrong character (1.1.1, fixed 1.1.2)
  # ============================================================
  sol_kb_types_wrong_char:
    type: solution
    text: |
      In **v1.1.1**, the keyboard layout editor's **"Types"** dropdown showed incorrect characters when selecting from the list — setting a key via the list populated the wrong value.

      **Fix:** Update to **v1.1.2+** where this is corrected.

      **Workaround on v1.1.1:** Use the **"Custom"** input box and type the character directly instead of selecting from the dropdown list.
    did_not_help: escalate_generic

  # ============================================================
  # §9.6  Holding D blocks mouse button F3 rebind
  # ============================================================
  sol_kb_d_blocks_mouse_f3:
    type: solution
    text: |
      Rebinding a mouse side button to trigger F3 works in most situations, but **fails when the D key (strafe right) is held simultaneously**. This also fails with vanilla AutoHotKey under the same conditions, which suggests it's a Minecraft or Windows input-handling quirk rather than a Toolscreen bug.

      There is currently **no Toolscreen-specific fix**. A bug report has been filed with Mojang. As a workaround, consider rebinding your strafe key or using a different key for your F3 trigger that doesn't conflict.
    did_not_help: escalate_generic

  # ============================================================
  # §9.10  No single toggle for typing mode / shift layers
  # ============================================================
  sol_kb_typing_toggle:
    type: solution
    text: |
      There is currently no built-in single key to **pause all rebinds while typing in chat** and resume them when done.

      The **"Toggle Key Rebinding"** hotkey (Ctrl+I → Advanced → Hotkeys → Toggle Key Rebinding) can disable/enable all rebinds, but you must press it manually before and after typing.

      **Planned feature:** Shift layer support would allow different key mappings to activate automatically by context. This is awaiting a ruling from speedrun.com moderators on legality before implementation.
    did_not_help: escalate_generic

  # ============================================================
  # §11.2  Alternative mode hotkey priority bug
  # ============================================================
  sol_mode_alt_hotkey_priority:
    type: solution
    text: |
      If you have a hotkey with an **alternative mode on Shift+key**, pressing the base key alone may incorrectly trigger the alternative mode swap first. The hotkey evaluation order is inverted — this is a **known bug** acknowledged by the developer.

      **Workaround:** Avoid using Shift-variant alternative modes on the same hotkey until a fix is released. Use separate hotkeys for each mode instead.
    did_not_help: escalate_generic

  # ============================================================
  # §11.3  Preemptive vs EyeZoom spike sizes (expected behaviour)
  # ============================================================
  sol_mode_preemptive_vs_eyezoom:
    type: solution
    text: |
      Preemptive mode and EyeZoom mode render at different internal chunk-load resolutions, so **entity spikes on the pie chart look larger in EyeZoom than in Preemptive**.

      This is **expected behaviour**, not a bug.

      - Use **Preemptive mode** for finding and identifying spikes
      - Use **EyeZoom mode** for precise angle measurement

      The different sizes are a consequence of the rendering approach and cannot be changed.
    did_not_help: escalate_generic

  # ============================================================
  # §12.3  Virtual camera FPS cost
  # ============================================================
  sol_obs_virtual_camera_fps:
    type: solution
    text: |
      The **virtual camera doubles the render workload** — Toolscreen renders the game once for your display and once for the virtual camera output. This causes a significant FPS drop, which is expected and confirmed by the developer.

      **Fix:** Only enable the virtual camera when you actively need it (Discord screenshare, streaming). Disable it for normal play:
      Ctrl+I → Advanced → Settings → Virtual Camera → Off
    did_not_help: escalate_generic

  # ============================================================
  # §12.4  General FPS lower with Toolscreen / mapless mirror perf
  # ============================================================
  sol_perf_mapless_mirror:
    type: solution
    text: |
      Toolscreen has a general FPS cost due to its rendering pipeline. Some specific things make it significantly worse:

      1. **Mapless mirror in Fullscreen mode** — this is the single biggest FPS culprit. Remove it from fullscreen if you don't need it there: Ctrl+I → Advanced → Modes → Fullscreen → Mirrors → remove the Mapless mirror group
      2. **Borderless mode** has lower performance than fullscreen — switch to fullscreen if FPS is critical
      3. **Virtual camera enabled** when not needed — disable it (see above)
      4. **Unused mirrors** — remove any mirror groups from modes where you don't need them

      Performance improvements are being worked on for future versions.
    did_not_help: escalate_generic

  # ============================================================
  # §14.2  Mirror accidentally deleted
  # ============================================================
  sol_mirror_deleted:
    type: solution
    text: |
      If you accidentally deleted a mirror or mirror group, **"Reset to Defaults"** only resets the mode layout — it does not restore mirrors.

      **Options:**
      1. **Restore from backup**: `C:\Users\<username>\.config\toolscreen\backups\` — find the most recent backup before the deletion and copy it over `config.toml`
      2. **Re-add the mirror manually**: Ctrl+I → Advanced → Modes → [Your Mode] → Mirrors → Mirror Groups → Add the relevant group (e.g., "Preemptive Pie")
      3. **Use a community config**: Check the `#mirrors-configs` channel in the Toolscreen Discord for pre-made mirror configurations you can paste in
    did_not_help: escalate_generic

  # ============================================================
  # §14.3  Mirror shows CPU name instead of E count
  # ============================================================
  sol_mirror_cpu_bleed:
    type: solution
    text: |
      If your E Counter mirror shows a CPU name or partial text instead of the entity count numbers, your CPU name on the F3 screen is **bleeding into the mirror's capture zone**.

      **Fix:**
      1. Increase your **Thin mode width** slightly (by 10–20 px) — this pushes the capture zone to where only the numbers appear
      2. Or adjust the **E Counter mirror's capture X offset and width** directly: Ctrl+I → Advanced → Modes → Thin → Mirrors → E Counter → adjust the capture region

      The correct position varies by CPU name length, so you may need to experiment.
    did_not_help: escalate_generic

  # ============================================================
  # §15.1  Cursor disappears after alt-tab
  # ============================================================
  sol_cursor_disappears:
    type: solution
    text: |
      If the mouse cursor disappears from the game window after alt-tabbing back in (or when a window overlay is active):

      1. **Alt-tab out and back in** — usually restores it
      2. **Toggle borderless off and back on** using your borderless hotkey
      3. If using OBS: switch from **Window Capture** to **Game Capture** — Window Capture can interfere with cursor visibility
      4. If it happens consistently with a window overlay active, try repositioning or resizing the overlay slightly

      This is an intermittent issue with no permanent fix yet.
    did_not_help: escalate_generic

  # ============================================================
  # §16.1  Auto-updater silently fails (Defender deletes new DLL)
  # ============================================================
  sol_autoupdate_silent_fail:
    type: solution
    text: |
      The auto-updater downloads the new version, but **Windows Defender's real-time protection silently deletes the new DLL** before it can be registered. The game then launches on the old version with no error message.

      **Signs this happened:** Toolscreen prompts to update, you accept, but the version number hasn't changed after relaunch.

      **Fix:**
      1. Temporarily disable **Windows Defender real-time protection** (Windows Security → Virus & threat protection → Real-time protection → Off)
      2. Run the update again (or download and install manually from GitHub)
      3. Re-enable real-time protection and add the exclusions:
         - `C:\Users\<username>\.config\toolscreen\`
         - The Toolscreen JAR and instance folder

      After exclusions are set, future auto-updates should work without needing to disable Defender.
    did_not_help: nsu_antivirus

  sol_faq_gif_background:
    type: solution
    text: |
      **GIF backgrounds are supported.** Use a `.gif` file (not `.mp4` or other video formats).
      Set it in Ctrl+I → Advanced → Modes → [Your Mode] → Background → select your `.gif` file.

      Note: Very large GIF files (high resolution or many frames) can cause lag. Keep them reasonably sized.
    did_not_help: escalate_generic

  sol_faq_autolaunch_ninb:
    type: solution
    text: |
      You can auto-launch NinjaBrainBot when Minecraft starts via Toolscreen's **prelaunch.txt** file.

      Add this line to `C:\Users\<username>\.config\toolscreen\prelaunch.txt` (create it if it doesn't exist):
      ```
      start "" javaw -jar "C:\path\to\ninjabrain-bot.jar"
      ```
      Replace the path with the actual location of your NinjaBrainBot jar.

      Toolscreen will execute this before Minecraft launches each time.
    did_not_help: escalate_generic

  sol_faq_transparent_fullscreen:
    type: solution
    text: |
      A **transparent window background in fullscreen** is technically impossible. In true fullscreen mode, there is nothing behind the game window to show through — the desktop and other windows are not rendered.

      In **borderless** mode, transparency is possible because the desktop is still composited behind the window. If you need a transparent effect, use borderless mode.
    did_not_help: escalate_generic

  sol_faq_mc121:
    type: solution
    text: |
      Toolscreen has **partial support for Minecraft 1.21.x**. The main known issue is that resizing (switching to Thin/Wide/EyeZoom mode) **breaks terrain rendering** — chunks may not load correctly or appear corrupted after a resize.

      This is actively being worked on. For now, use **1.16.1** or another fully-supported version if you need reliable resize functionality.
    did_not_help: escalate_generic

  # ============================================================
  # §18  Boat eye / sensitivity common confusion (FAQ)
  # ============================================================
  sol_faq_boateye_setup:
    type: solution
    text: |
      Common boat eye setup mistakes:

      **You no longer need to change DPI or Windows cursor speed** for boat eye with Toolscreen. Set your MC sensitivity to `0.02291165` (in both `options.txt` and `standardsettings.json`), then use Toolscreen's **Global Sensitivity multiplier** to match your preferred cm/360. Use the calculator: https://qmaxxen.github.io/Toolscreen-sens-calc/

      **Raw input must always be ON.** Never turn it off. Use the EyeZoom Sensitivity Override slider to slow down measuring sensitivity.

      **FOV must be 30** when measuring eyes. This is the single most common cause of missed throws — double-check it every session.

      **Enhanced Pointer Precision** (Windows mouse acceleration) must be **OFF**: Windows Settings → Bluetooth & devices → Mouse → Additional mouse settings → Pointer Options → uncheck "Enhance pointer precision".

      **NinjaBrainBot sensitivity** (v1.13+) must match the value in `options.txt` — use `0.02291165`. Do not leave it at the old default of `0.012727`.

      **Crosshair correction in NinjaBrainBot must be 0** — non-zero values from Jingle tutorials will give wrong angles with Toolscreen.
    did_not_help: escalate_generic



